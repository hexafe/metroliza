[package]
name = "metroliza_chart_native"
version = "0.1.0"
edition = "2021"

[lib]
name = "_metroliza_chart_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
image = { version = "0.25", default-features = false, features = ["png"] }
