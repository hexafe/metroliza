"""Task-owned, windowless frozen process for the native identity discriminator."""

import time


if __name__ == "__main__":
    # Keep the resumed process observable without input, output or child processes.
    time.sleep(2)
